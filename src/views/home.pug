extends base.pug

block content
  h2 Welcome here
  ul
    each video in videos
      // video는 아무이름일수 있지만, videos는 controller에서 보내주는 배열의 변수명과 같아야해! each 단수 in 복수 보통 이런 형태로 쓰지
      li=video
      // li #{video} 이것도 같지만, "변수만"사용 할꺼라 =로 넣어줌
    else 
      li Sorry nothing found.